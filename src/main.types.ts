export type SkuEntry = { Type: string; SKU: string };

export type ProductData = {
    sku: string;
    source: string;
    title: string;
    description: string;
    price: string;
    reviewsAndRating: string;
}